{
  "name": "personal-blog-content",
  "private": true,
  "scripts": {
    "postinstall": "cd engine && npm install",
    "dev": "cd engine && npm run dev",
    "build": "cd engine && npm run build",
    "preview": "cd engine && npm run preview",
    "check": "cd engine && npm run check",
    "deploy": "npx wrangler@latest deploy",
    "deploy:prod": "npx wrangler@latest pages deploy ./engine/dist --project-name=personal-blog --branch=main",
    "deploy:preview": "npx wrangler@latest pages deploy ./engine/dist --project-name=personal-blog --branch=preview"
  }
}
