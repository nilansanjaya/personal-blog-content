# Cloudflare Pages configuration for The Invincible Blog
# Deploy via Git integration or with: npx wrangler pages deploy ./engine/dist --project-name=personal-blog

[build]
command = "cd engine && npm run build"
directory = "engine/dist"

[build.environment]
NODE_VERSION = "20"